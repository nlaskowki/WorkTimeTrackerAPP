<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.worktimetrackerapp">

    <application
        android:name=".DB"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="WorkTimeTracker"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GUI_Interfaces.SignIn_Controller"
            android:label="WTT">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".GUI_Interfaces.SignUp_Controller" />
        <activity android:name=".GUI_Interfaces.PopUpWindows" />


    </application>

</manifest>